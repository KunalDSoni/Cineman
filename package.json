{
  "name": "cineman-reel",
  "version": "1.0.0",
  "private": true,
  "description": "CineMan — The Reel, wrapped as a native iOS app for real CoreHaptics. The web experience is unchanged; the native shell adds a Taptic Engine bridge.",
  "scripts": {
    "build:web": "node scripts/sync-web.mjs",
    "sync": "npm run build:web && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
